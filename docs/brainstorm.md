# 项目脑暴记录 - Loom

## 项目概述

将本地 AI Runtime（如 Claude Code、OpenCode）作为底层能力，通过 Daemon + Backend + Client 架构，打造成一个具有"氛围感"的虚拟陪伴产品。

**核心定位**：解决现有 AI Runtime"工作属性太重"的问题，让 AI 交互更具娱乐化和二次元体验。

**目标用户**：具备代码能力且喜欢亚文化的重度用户

---

## 产品形态

### MVP 功能

| 功能 | 优先级 | 说明 |
|------|--------|------|
| 对话能力 | P0 | AI 对话 + Tools 执行 |
| lofi 音乐 | P0 | 背景音乐播放 |
| 角色占位 | P1 | 简单图片占位，预留 Live2D 接口 |

### UI 设计

**分层架构**：

```
┌─────────────────────────────────┐
│  Layer 4: 控制层 (最小化/设置)   │  ← 始终可触达
├─────────────────────────────────┤
│  Layer 3: 对话层 (可收起)        │  ← 对话时展开
├─────────────────────────────────┤
│  Layer 2: 角色层 (占位符)        │  ← 始终显示
├─────────────────────────────────┤
│  Layer 1: 背景层 (GIF/视频)      │  ← 始终播放
└─────────────────────────────────┘
```

**两种状态**：

| 状态 | 背景 | 角色 | 对话框 |
|------|------|------|--------|
| 空闲 | 全屏 GIF | 中央显示 | 收起或透明度 10% |
| 对话 | 模糊/变暗 | 缩小偏移 | 完全显示，聚焦 |

**MVP 界面**：

```
┌─────────────────────────────────────────┐
│  🌙 [lofi 播放条]              [设置] ⚙️ │  ← 顶部常驻
├─────────────────────────────────────────┤
│                                         │
│           [GIF/视频背景]                 │  ← 主体
│                                         │
│   ┌─────────────┐                       │
│   │  角色占位符  │                       │
│   └─────────────┘                       │
│                                         │
│  ┌────────────────────────────────────┐ │
│  │ 💬 对话框 (输入框)                  │ │  ← 底部，可收起
│  └────────────────────────────────────┘ │
└─────────────────────────────────────────┘
```

---

## 技术架构

### 技术栈

| 层级 | 技术选择 | 说明 |
|------|----------|------|
| 前端 | TanStack Start | 全栈框架 |
| 后端 API | Bun | 高性能 JavaScript 运行时 |
| Daemon | Go | 本地 CLI，编译成单文件二进制 |
| WebSocket | Centrifugo | 生产级 WS 服务器 |
| 数据库 | PostgreSQL | 持久化存储 |

### 整体架构

```
┌────────────────────────────────────────────────────────────────────┐
│                         用户机器 (localhost)                        │
│                                                                     │
│  ┌─────────────┐                  ┌──────────────────────────┐    │
│  │   Runtime   │◀── SDK ─────────▶│         Daemon           │    │
│  │ Claude Code │    (驱动层)       │  ┌──────────────────┐   │    │
│  │  OpenCode   │                  │  │  Runtime Adapter │   │    │
│  └─────────────┘                  │  │  (Claude/OpenCode)│   │    │
│                                    │  │  - Chat          │   │    │
│                                    │  │  - ExecuteTool  │   │    │
│                                    │  │  - ListCaps     │   │    │
│                                    │  └──────────────────┘   │    │
│                                    │  ┌──────────────────┐   │    │
│                                    │  │   WS Client      │   │    │
│                                    │  │  (连 Backend)    │   │    │
│                                    │  └──────────────────┘   │    │
│                                    └────────────┬─────────────┘    │
└─────────────────────────────────────────────────┼──────────────────┘
                                                  │ WebSocket
                                                  ▼
┌──────────────────────────────────────────────────────────────────────┐
│                           云端                                        │
│                                                                       │
│   ┌─────────────┐         ┌─────────────────┐    ┌────────────────┐ │
│   │   Client    │◀───────▶│    Backend     │◀───▶│  Centrifugo    │ │
│   │  (Web UI)   │   HTTP  │    (Bun)       │ WS  │  (WS Server)   │ │
│   │             │         │  - API         │    │                │ │
│   │ TanStack    │         │  - 业务逻辑    │    │  - 用户频道    │ │
│   │  Start      │         │  - 消息转发    │    │  - Daemon 频道 │ │
│   └─────────────┘         └─────────────────┘    └────────────────┘ │
└──────────────────────────────────────────────────────────────────────┘
```

### 通信协议

| 组件对 | 协议 | 说明 |
|--------|------|------|
| Client ↔ Backend | HTTP + WebSocket | Web UI 访问云端 API 和 WS |
| Daemon ↔ Backend | WebSocket | 持久连接，双向通信，心跳保活 |
| Runtime ↔ Daemon | 子进程/SDK | 调用本地 Claude Code/OpenCode |
| Client ↔ Daemon | HTTP | 开发调试用（可选） |

### Daemon 设计

**职责**：
- 统一 Runtime 接口（适配 Claude Code、OpenCode）
- 管理本地 AI 能力（Tools、Skills）
- 与 Backend 建立 WebSocket 持久连接

**API 设计**：

```bash
# 对话（流式）
POST /api/chat
Body: { "messages": [...], "tools": [...] }
Response: Stream (SSE)

# 执行 Tool
POST /api/tools/execute
Body: { "name": "Bash", "input": { "command": "ls" } }
Response: { "output": "...", "error": null }

# 列出可用 Tools
GET /api/tools

# 健康检查
GET /health
```

**运行时支持**：
- 通过配置切换：`runtime: "claude-code"` 或 `"opencode"`
- 内部统一抽象层，底层调用不同的 Runtime
- 后续加新 Runtime 只需加适配器

---

## 待讨论

- [x] 产品名称：**Loom**
- [x] 技术栈：TanStack Start + Bun + PostgreSQL + Centrifugo
- [x] 部署形态：Daemon 本地 + Backend 云端托管
- [x] MVP 优先级：对话 + lofi 音乐 + 角色占位
- [ ] 人设系统（角色性格怎么配置）
- [ ] 记忆系统（后期接入，不自己设计）

---

## 参考项目

- **TanStack Start**：全栈框架
- **Centrifugo**：生产级 WebSocket 服务器
- **lofi.cafe**：UI 风格参考
- **Tavern AI**：人设导入参考
- **Claude Code**：Runtime 参考

---

## 设计文档

技术设计文档已保存至：`docs/superpowers/specs/2026-04-15-loom-design.md`
