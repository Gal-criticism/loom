name: Release

on:
  workflow_dispatch:
    inputs:
      version:
        description: '版本号 (如 0.1.0)'
        required: true
        default: 'latest'
        type: string
  push:
    tags:
      - 'v*'

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata
        id: metadata
        run: |
          REPO=$(echo "${{ github.repository }}" | tr '[:upper:]' '[:lower:]')
          echo "REPO=$REPO" >> $GITHUB_OUTPUT
          echo "VERSION=${{ github.event.inputs.version || github.ref_name }}" >> $GITHUB_OUTPUT

      - name: Build and push Backend
        uses: docker/build-push-action@v5
        with:
          context: ./backend
          push: true
          tags: |
            ghcr.io/${{ steps.metadata.outputs.REPO }}/backend:${{ steps.metadata.outputs.VERSION }}
            ghcr.io/${{ steps.metadata.outputs.REPO }}/backend:latest
          cache-from: type=registry,ref=${{ steps.metadata.outputs.REPO }}/backend:latest
          cache-to: type=inline

      - name: Build and push Client
        uses: docker/build-push-action@v5
        with:
          context: ./client
          push: true
          tags: |
            ghcr.io/${{ steps.metadata.outputs.REPO }}/client:${{ steps.metadata.outputs.VERSION }}
            ghcr.io/${{ steps.metadata.outputs.REPO }}/client:latest
          cache-from: type=registry,ref=${{ steps.metadata.outputs.REPO }}/client:latest
          cache-to: type=inline

      - name: Summary
        run: |
          echo "发布版本: ${{ steps.metadata.outputs.VERSION }}"
          echo "镜像地址:"
          echo "  - ghcr.io/${{ steps.metadata.outputs.REPO }}/backend:${{ steps.metadata.outputs.VERSION }}"
          echo "  - ghcr.io/${{ steps.metadata.outputs.REPO }}/client:${{ steps.metadata.outputs.VERSION }}"
